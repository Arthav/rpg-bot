const { logToFile } = require("../logger");
const { userExists, createUser } = require("../db");
const { keyboardTemplate } = require("../layout/keyboardLayout");

function messageHandler(bot) {
  
}

module.exports = messageHandler;
